
## 專案定位

**目標受眾**：CS 學生、演算法學習者、對空間資料結構有興趣的開發者  
**核心體驗**：讓使用者在同一個畫面上操作並即時看到兩種結構的差異，而非被動閱讀  
**記憶點**：畫面永遠是左右對切的——就像 KD-Tree 本身切空間的方式

---

## 美學方向

**風格**：Monochrome Technical — 工業感、精準、像工程師的白板  
**色調**：接近純白背景，兩個主色各代表一種樹（例如 KD-Tree 用深靛藍、Quadtree 用墨綠），  
其餘全用灰階。高對比的 monospace 字型貫穿全站。  
**字型**：
- Display / 標題：`IBM Plex Mono` 或 `Space Mono`（工程感、等寬）
- Body：`DM Mono` 或 `Geist Mono`
- 避免：Inter、Roboto、任何圓潤無特色的 sans-serif

**動態感**：切割線出現時要有「切下去」的動畫感（從邊緣往內延伸），  
而不是 fade-in。節點出現用 stagger。

---

## 頁面結構（單頁應用）

```
┌─────────────────────────────────────────────────────┐
│  NAVBAR                                             │
├─────────────────────────────────────────────────────┤
│  HERO                                               │
├─────────────────────────────────────────────────────┤
│  CANVAS  (主互動區，佔最大空間)                        │
│  ┌──────────────────┬──────────────────┐            │
│  │   KD-Tree        │   Quadtree       │            │
│  │   (左半)         │   (右半)         │            │
│  └──────────────────┴──────────────────┘            │
├─────────────────────────────────────────────────────┤
│  CONTROLS                                           │
├─────────────────────────────────────────────────────┤
│  STATS PANEL                                        │
├─────────────────────────────────────────────────────┤
│  EXPLAINER  (可選，滾動展開)                          │
└─────────────────────────────────────────────────────┘
```

---

## 各區塊細節

### 1. Navbar
- 左側：`PARTITION` 標誌（monospace 大寫，字距拉開）
- 右側：`KD-Tree` • `Quadtree` 兩個 tag，各有對應主色底色
- 極細底線（1px），無陰影
- 高度：48px，sticky

---

### 2. Hero
- 高度：螢幕高度的 30%
- 主標題：`Two ways to cut space.`（大字，等寬字型）
- 副標題：一行說明兩者差異的短句
- 背景：細格線紋理（CSS grid pattern），暗示空間切割
- 無按鈕，直接引導視線往下進入 Canvas

---

### 3. Canvas（主互動區）⭐ 最重要

畫面永遠是**左右對分**，中間有一條 1px 分割線（帶顏色漸層，上 KD-Tree 色、下 Quadtree 色）。

**左側 — KD-Tree**
- 標籤：左上角固定，`KD-TREE`，對應主色
- 空間：使用者點擊放置資料點
- 切割線：每次點擊後自動重算，切割線從邊緣延伸進來（animation）
- 切割線顏色：按深度漸淡（第1層最深，越深越淺）
- 節點高亮：hover 任一點，顯示它在樹中的深度 badge

**右側 — Quadtree**
- 標籤：右上角固定，`QUADTREE`，對應主色
- 同一組資料點鏡像顯示（或使用者也可各自操作）
- 格子切割：超過閾值才四分，動畫是「十字線從中心往四邊擴展」
- 空格：淡色填充，區別於有點的格子

**Canvas 互動**
- 點擊空白處：新增資料點（兩側同步）
- 點擊現有點：刪除
- 拖拉點：移動，兩側即時更新結構
- 右上角小工具列：清除全部、隨機生成 N 個點、均勻/叢集分布切換

---

### 4. Controls
- 水平排列，居中
- `點數上限` slider（10–200）
- `Quadtree 分裂閾值` slider（1–8 個點才切）
- `顯示深度` toggle（切換是否用顏色標示深度層次）
- `動畫速度` slider
- 樣式：極簡 pill-shaped，無框線，只有底線或填色

---

### 5. Stats Panel
兩欄並排，各對應一個樹，即時更新：

| KD-Tree | Quadtree |
|---------|----------|
| 樹高 | 樹高 |
| 節點數 | 節點數（含空節點） |
| 最後一次最近鄰搜尋：訪問節點數 | 同左 |
| 建構時間 (ms) | 建構時間 (ms) |

數字變動時要有「flip」或「count-up」動畫，不要靜態換數字。

**搜尋示範**：使用者 hover 畫布時，顯示「若查詢這個位置，會訪問哪些節點」（高亮對應節點）。

---

### 6. Explainer（可選）
- 點擊「Learn more」後展開，或直接滾動到底
- 每個概念配上對應的小 canvas 動畫（非靜態圖）
- 三個主題：切割邏輯 / 最近鄰搜尋 / 動態更新
- 底部：連回主 Canvas 的 anchor link

---

## 響應式行為

| 寬度 | 佈局 |
|------|------|
| ≥ 1200px | 左右並排 Canvas，全功能 |
| 768–1199px | 左右並排但 Stats Panel 移到下方 |
| < 768px | 上下堆疊（KD-Tree 在上、Quadtree 在下），Controls 簡化 |

---

## 顏色系統

```
--color-bg:          #F8F7F4   /* 暖白，不刺眼 */
--color-surface:     #EFEFEB
--color-border:      #D0CFC8
--color-text:        #1A1A18
--color-text-muted:  #888880

--color-kd:          #1B3A6B   /* 深靛藍，KD-Tree */
--color-kd-light:    #E8EEF7
--color-qt:          #0D4A30   /* 墨綠，Quadtree */
--color-qt-light:    #E3F0E9

--color-point:       #C84B2F   /* 資料點，珊瑚紅，兩側共用 */
--color-highlight:   #F0A500   /* hover/選取高亮 */
```

---

## 動畫規格

| 動作 | 動畫 |
|------|------|
| 新增點 | 從 0 scale 到 1，easeOutBack，120ms |
| KD 切割線出現 | 從邊緣延伸至另一邊，easeInOut，200ms |
| QT 格子分裂 | 十字線從格子中心往四角擴展，150ms |
| 刪除點 | scale 縮到 0 + opacity 0，100ms |
| Stats 數字更新 | count-up 或 flip，300ms |
| hover 搜尋路徑 | 節點依訪問順序逐一點亮，每個 30ms |

所有動畫加 `prefers-reduced-motion` fallback（直接呈現最終狀態）。

---

## 技術建議

- **框架**：React（方便管理兩側狀態同步）
- **Canvas 渲染**：HTML Canvas API 或 SVG（點數 < 500 用 SVG 較易互動，> 500 考慮 Canvas）
- **狀態管理**：`useReducer`，兩棵樹的狀態集中管理
- **效能**：點數超過 100 時，KD-Tree 重建用 `requestAnimationFrame` 排程，避免卡頓
